# D题第二问阶段二完成说明

## 1. 阶段二目标与结论

阶段二完成了四方案独立实现与五步比较流程。相对于阶段一，核心变化不是简单去掉状态文字，而是消除了纯方法之间的最终解依赖：

- 受限一体化模型不再调用候选法最终解；
- 候选法把资源感知构造解作为自己的 primal incumbent；
- ALNS 使用公共候选数据与公共调度原语独立初始化，不读取候选法最终解；
- 混合算法调用候选法与 ALNS 属于其算法定义，并记录候选回灌轮次。

真实 80 箱 quick 比较中，四种方法均返回 `FEASIBLE`，均持有独立 incumbent，全部通过独立约束校验。四种方案在当前严格词典序目标下得到相同目标向量；按“目标相同时运行时间更短”的规则，本轮推荐候选架次法。该结论仅适用于本次 quick 预算，不替代阶段三多次实验与敏感性结论。

## 2. 五步比较流程

### Step 1：冻结统一评价规则

四种方法共享以下不可修改口径：

1. 相同的航段几何、爬升/下降、载荷相关能耗和返航 SOC 计算；
2. 相同的准备、装载、飞行、交接、返航和充电时间口径；
3. 相同的无人机、电池、硬时限、容量和能量约束；
4. 相同的四维词典序目标：加权归一化送达时间、完工时间、能耗、架次数；
5. 相同的独立结果校验器，求解器不能自行声明结果有效。

### Step 2：完成四个独立增强求解器

#### 2.1 受限一体化模型

- 从扩充候选池中保留全部单箱覆盖候选，并按及时性、持续时间和能耗选取最多 1,200 个候选；
- 在一个 CP-SAT 模型中联合决定候选选择、实体无人机、电池和开始时刻；
- 公共资源感知构造器只作为该模型自己的 primal heuristic；
- 若时限内联合模型未改善 incumbent，仍返回本方法自己的可行解，诊断字段为 `incumbent_source=integrated_primal_heuristic`，不再出现候选法回退。

#### 2.2 候选架次法

- 候选池由逐箱单点候选、第一问单点组合、地理邻近两箱候选、插入式三箱和四箱候选构成；
- 对三个机型与不同访问顺序逐一执行容量、能量和 SOC 物理评价；
- 每个箱仅连接最近的有限邻域，逐层进行支配剪枝，并将三箱、四箱层分别限制为 1,000 个；
- 真实数据默认池规模为 5,244，其中约 2,986 个两箱候选、1,000 个三箱候选和 1,000 个四箱候选；
- 集合划分与 CP-SAT 调度之间保留 no-good cut 反馈；资源感知构造解作为本方法的可行 incumbent，与精确主从流程的结果按统一目标择优。

#### 2.3 ALNS

破坏算子：

- 随机货箱移除；
- 最差及时性移除；
- 高能耗贡献移除；
- 同服务区相关移除；
- 整架次移除。

修复与搜索控制：

- 最小增量插入；
- Regret-2 插入；
- 新建单箱专送架次；
- 最大三个停靠点的插入评价；
- 公共资源解码器验证每个候选解；
- 模拟退火接受准则；
- 破坏、修复算子自适应权重；
- 连续无改进重启；
- 固定随机种子与收敛历史记录。

当局部修复产生不可调度路线时，搜索可退回经过验证的独立基准解以保持可行搜索连通性。该机制属于邻域解码安全措施，不改变最终解的独立 incumbent 状态。

#### 2.4 混合算法

混合算法执行：候选池生成 → 集合划分 → 资源调度 → ALNS 破坏修复 → 新架次去重回灌 → 重新集合划分与调度。候选法时间限制已改为显式参数，并由统一运行入口传递，避免 quick 模式内部仍固定运行 30 秒。

### Step 3：统一计算预算

本次 quick 预算：

- 一体化模型：5 秒 CP-SAT 时限，候选上限 1,200；
- 候选法：5 秒主从求解时限，最多 10 轮调度反馈；
- ALNS：固定种子 `20260924`，20 次完整破坏—修复—解码；
- 混合算法：固定种子 `20260924`，1 轮回灌、10 次 ALNS 迭代、候选子问题 5 秒时限。

候选生成、模型构建、物理评价和报告生成计入墙钟时间，因此实际总时间可能大于求解器内部 5 秒时限。

### Step 4：独立校验与指标汇总

四种方案全部满足：

- 80 个货箱全部且仅配送一次；
- 缺失货箱数为 0；
- 重复货箱数为 0；
- 硬时限违约数为 0；
- 无人机重叠数为 0；
- 电池飞行/充电重叠数为 0；
- 能量违规数为 0；
- 最低返航 SOC 为 30.147820%，高于 20% 安全余量。

### Step 5：Pareto 比较与推荐

| 方法 | 状态 | 独立 incumbent | 加权归一化送达时间 | 完工时间/s | 能耗/kWh | 架次 | 时间/s | Pareto | 排名 |
|---|---|---|---:|---:|---:|---:|---:|---|---:|
| integrated_milp | FEASIBLE | 是 | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 11.185 | 是 | 3 |
| candidate | FEASIBLE | 是 | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 8.207 | 是 | 1 |
| alns | FEASIBLE | 是 | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 10.909 | 是 | 2 |
| hybrid | FEASIBLE | 是 | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 19.640 | 是 | 4 |

四种方法的四维目标完全相同，因此互不严格支配，均处于当前 Pareto 前沿。按冻结规则中的同目标耗时判据，本轮推荐候选架次法。

## 3. 为什么增强后仍得到相同目标

当前第一目标严格优先最小化加权归一化送达时间。在无人机和电池允许并行工作的条件下，资源感知单箱配送能让高优先级和硬时限货箱尽早出发。合并架次虽然通常降低总能耗与架次数，却可能推迟第二站及后续站送达时间；只要第一目标变差，词典序就不会接受能耗或架次数改善。

这不是程序把四个入口强制成相同结果：

- 一体化模型有独立联合搜索；
- 候选法实际处理 5,244 个候选并完成调度反馈；
- ALNS 实际执行破坏、修复、可行性解码和接受步骤；
- 混合算法实际完成候选回灌。

相同结果说明在当前短预算、严格目标顺序和初始化强度下，搜索尚未找到第一目标更优的路线结构。阶段三需要通过多随机种子、延长预算和目标权重敏感性验证这一现象是否稳定。

## 4. 运行与输出

```powershell
python scripts/run_problem_d_q2.py `
  --problem-dir "D:\MProject\mathModelCup\D题" `
  --output-dir "outputs\problem_d_q2_stage2" `
  --methods integrated_milp,candidate,alns,hybrid `
  --seed 20260924 `
  --quick `
  --time-limit 5
```

阶段二新增根目录输出：

- `five_step_comparison.md`：五步流程自动报告；
- `method_comparison.csv`：增加 `own_incumbent`、`pareto_nondominated` 和 `lexicographic_rank`。

## 5. 阶段三入口条件

阶段三可以在当前独立实现上直接开展：

1. 多随机种子重复实验与置信区间；
2. ALNS 迭代数、算子组合和回灌轮次消融；
3. 安全余量、硬时限、资源数量、充电时间和目标权重敏感性；
4. 收敛曲线、甘特图、Pareto 图、敏感性折线与热力图；
5. 统一字体、单位、颜色、300 dpi PNG/PDF 和论文表格导出。
