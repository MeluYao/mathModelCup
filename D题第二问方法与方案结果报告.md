# D题第二问方法与方案结果报告

## 1 结论

本次修复已经解决“四个算法在报告层被强制替换为同一个解”的问题。现在的流程是：先由分组局部搜索生成一个共同高质量可行种子，再把该种子真正传入受限一体化 CP-SAT、候选架次法、ALNS 和混合算法内部；各方法在自身搜索过程中决定保留或改进种子，求解结束后不再进行共享解覆盖。

在 quick 预算、20% 返航安全余量和统一四维词典序目标

$$
\min (Z_1,\;M,\;E,\;K)
$$

下，四种算法在当前 quick 预算内均保留同一个经过独立校验的 22 架次种子：

$$
(Z_1,M,E,K)
=(0.5163286171,\;9408.4439\text{ s},\;64.8317281\text{ kWh},\;22).
$$

终值相同不再是报告层覆盖造成的：四个求解器都实际接收该种子，并分别记录 `seed_retained=true`、`native_improved_seed=false` 和 `incumbent_source=provided_seed`。50 次和 200 次混合迭代的补充检查也没有找到严格改进。因此本轮推荐 ALNS 作为最快原生搜索入口，同时把 22 架次方案称为当前最好已知可行解，不能表述为已证明全局最优。

## 2 使用的方法

### 2.1 统一物理与 GIS 计算核

四种算法共用同一数据加载、物理计算、资源排程和独立校验器：

1. 从服务区与调度中心坐标、DEM 数据构造有向航段矩阵；
2. 分方向累计爬升和下降，避免把往返高程影响当成完全对称；
3. 按各航段剩余载荷计算飞行时间、等效航程和能耗；
4. 每次卸货后递减载荷，再计算后续航段；
5. 检查质量、体积、返航 SOC、硬时限、实体无人机占用和电池充电周转；
6. 从最终逐箱送达记录重新计算四维目标，不采用算法内部代理分数作为最终成绩。

### 2.2 共同高质量种子

共同种子由以下步骤构造：

1. 同服务区首次适应递减分组；
2. 每次插入都执行完整架次物理评估；
3. 用确定性双资源调度器分配无人机、电池和起飞时刻；
4. 提取迟到物资并重新分组或换型；
5. 执行整组移动、单箱移动、合并、拆分、路线重排和换机型邻域；
6. 按发布目标顺序 $(H,Z_1,M,E,K,W)$ 选择搜索状态，其中 $H$ 为硬时限违反数，$W$ 为加权软迟到。

该方法生成 22 架次、80 箱完整覆盖、硬时限违反为 0 的种子，最低返航 SOC 为 21.4675%。

### 2.3 受限一体化 CP-SAT

CP-SAT 同时决定候选架次选择、起飞时刻、实体无人机和电池分配，并用 `NoOverlap` 表达两类可复用资源约束。修复后：

- 种子的每条架次都无条件进入候选池，不会被 1200 个候选上限截掉；
- 种子的选择、开始时刻、飞机和电池分配作为 CP-SAT hint；
- 求解器未找到更优解时，明确记录 `provided_seed`，不再伪装成独立新解。

### 2.4 候选架次法

先生成物理可行架次列，再求解集合划分，并对所选组合执行资源排程；资源不可行的组合通过 no-good cut 排除。修复后，种子架次列被并入候选池并作为 incumbent，集合划分与排程结果只有严格改善统一目标时才替换种子。

### 2.5 ALNS

ALNS 在“架次集合 + 确定性资源解码”空间中使用随机移除、最差及时性移除、高能耗移除、同区移除、整架次移除，以及最小增量和 regret-2 修复。修复后 ALNS 从 22 架次共同种子启动，修复失败只拒绝当前候选，不会破坏已保存的最好解。

### 2.6 混合算法

混合算法把候选法、ALNS 和候选池刷新串联：候选法以共同种子为 incumbent，ALNS 从当前最好解继续搜索，新架次再反馈给候选池，最后按真实四维目标选择。本轮 quick、50 次和 200 次迭代均未严格改进种子。

## 3 四方案结果

| 方法 | 校验 | $Z_1$ | 完工时间/s | 能耗/kWh | 架次 | 原生时间/s | 来源 | 是否改进种子 |
|---|---:|---:|---:|---:|---:|---:|---|---:|
| 受限一体化 CP-SAT | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 4.802 | `provided_seed` | 否 |
| 候选架次法 | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 5.394 | `provided_seed` | 否 |
| ALNS | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 2.745 | `provided_seed` | 否 |
| 混合算法 | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 7.744 | `provided_seed` | 否 |

四种方案都通过独立校验，并且全部在自身原生流程中保留种子。结果相同只说明本预算下四种方法都没有找到严格更优解，不再说明它们被报告程序改成相同。目标并列时 ALNS 运行时间最短，因此本轮工程推荐 ALNS；候选法和 CP-SAT仍分别承担可解释分层求解与精确模型对照的作用。

![四方法目标比较](outputs/problem_d_q2_stage3/figures/fig01_method_objectives.png)

![四方法运行时间比较](outputs/problem_d_q2_stage3/figures/fig02_method_runtime.png)

## 4 相对原 80 架次缺陷的改善

原生单箱安全基线为

$$
(1.0945607062,\;18824.5834,\;191.6670288,\;80).
$$

| 指标 | 80 架次基线 | 22 架次当前解 | 改善 |
|---|---:|---:|---:|
| $Z_1$ | 1.094561 | 0.516329 | 52.83% |
| 完工时间/s | 18824.58 | 9408.44 | 50.02% |
| 能耗/kWh | 191.6670 | 64.8317 | 66.17% |
| 架次数 | 80 | 22 | 72.50% |

原四算法的主要缺陷已经解决：

- 不再从单箱解开始后因一次修复失败长期困在 80 架次；
- 高质量多箱结构不会在候选截断或候选剪枝中丢失；
- 四算法使用同一最终目标顺序比较解；
- 报告不再在求解结束后替换算法结果；
- 来源字段能够区分“保留种子”和“原生改进”。

仍未完全解决的部分是搜索强度：CP-SAT 候选池仍不完备，候选法的代理成本仍不能完全表达资源拥塞，ALNS quick 预算只有 20 次迭代。因此“没有改进种子”表示本预算下没有找到更优解，不表示算法或种子已经达到全局最优。

## 5 多随机种子与敏感性分析

ALNS 使用 20260924—20260933 共 10 个种子，每次从共同种子出发运行 20 次迭代。10 次全部通过校验，全部保留种子：

| 指标 | 结果 |
|---|---:|
| 实验次数 | 10 |
| 校验通过 | 10/10 |
| $Z_1$ 均值 | 0.5163286171 |
| $Z_1$ 标准差 | 0 |
| 平均搜索时间 | 0.2731 s |
| 最短 / 最长搜索时间 | 0.2017 / 0.3948 s |
| 改进种子的次数 | 0 |

零方差只能说明 20 次迭代的 quick 邻域稳定，不能证明局部最优或全局最优。

敏感性分析共 14 个场景，13 个找到可行解。基准 20% 安全余量下为 22 架次；15% 余量可得到 21 架次；资源降到 50% 时当前启发式未找到满足硬时限的排程。详细结果见 [sensitivity_analysis.csv](outputs/problem_d_q2_stage3/sensitivity_analysis.csv)。

![ALNS 多随机种子结果](outputs/problem_d_q2_stage3/figures/fig04_alns_multiseed.png)

![敏感性分析](outputs/problem_d_q2_stage3/figures/fig05_sensitivity.png)

## 6 物理与 GIS 严谨性

当前实现的严谨性体现在：所有方法共享同一物理核；航段有方向；能耗随载荷和累计爬升变化；每个架次逐站卸载；返航 SOC、充电时间、无人机占用和电池占用均进入校验；最终解重新从明细计算指标。80 箱均恰好送达一次，硬时限、资源重叠和能量违反均为 0。

但模型仍有明确边界：DEM 和坐标精度决定地形距离精度；当前未显式模拟实时风场、降雨、临时禁飞区、通信盲区和动态道路/空域风险；飞行路径是基于预处理航段的规划近似，不等同于真实飞控轨迹。因此它适合竞赛层面的可复现优化与方案比较，不应表述为无需现场校核即可执行的飞行计划。

## 7 与合作者方案的关系

合作者目录中的 21 架次结构经本项目同一物理核回放后约为

$$
(0.5469982,\;8825.00\text{ s},\;63.0788\text{ kWh},\;21).
$$

它的架次数更少、完工时间更短，但 $Z_1$ 明显大于当前 22 架次解。两者并不矛盾：合作者方案更偏向紧凑架次，本项目发布目标严格把加权归一化送达时间放在首位。若业务把架次数置于及时性之前，本项目另有 15 架次紧凑方案；若按当前论文词典序，应选择 22 架次解。

## 8 能否证明最优

不能。当前只能证明：

1. 所有报告方案满足程序中定义的物理、容量、资源、时限和唯一覆盖约束；
2. 四种算法在相同种子和 quick 预算下均没有找到更优四维目标；
3. 50 次和 200 次混合迭代的补充运行也保留了种子。

不能证明全局最优的原因包括：候选架次没有完整枚举，CP-SAT 只求解受限候选模型且没有给出原问题 gap=0 的上下界，ALNS 和混合算法是启发式，quick 迭代数较小。

若要给出严格最优性证明，需要完整列生成或可证明完备的候选枚举，并让集合划分与资源排程主问题获得匹配下界；或者构造覆盖原问题全部决策的 MILP/CP-SAT，并以严格 gap=0 结束。

## 9 输出与复现

- 四方案比较：[method_comparison.csv](outputs/problem_d_q2_stage3/baseline/method_comparison.csv)
- 各方案明细：[baseline](outputs/problem_d_q2_stage3/baseline)
- 多种子实验：[alns_repetitions.csv](outputs/problem_d_q2_stage3/alns_repetitions.csv)
- 敏感性分析：[sensitivity_analysis.csv](outputs/problem_d_q2_stage3/sensitivity_analysis.csv)
- 图表目录：[figures](outputs/problem_d_q2_stage3/figures)

```powershell
python scripts/run_problem_d_q2.py `
  --problem-dir "D:\MProject\mathModelCup\D题" `
  --output-dir "outputs\problem_d_q2_stage3" `
  --quick --time-limit 5

python scripts/run_problem_d_q2_experiments.py `
  --problem-dir "D:\MProject\mathModelCup\D题" `
  --output-dir "outputs\problem_d_q2_stage3" `
  --quick --time-limit 5
```
