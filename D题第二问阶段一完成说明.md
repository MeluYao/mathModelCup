# D题第二问阶段一完成说明

## 1. 阶段结论

阶段一已经形成四种可执行方案的最小可行版本：受限一体化模型、候选架次法、ALNS 和候选池—ALNS 混合算法。四种方案共用同一套数据模型、航段物理计算、架次评价、无人机/电池调度、目标函数和独立校验器，因此结果口径一致。

在 2026-09-24 的短时真实数据验收中，四个入口均返回完整可行解，80 个货箱全部且仅配送一次，无硬时限违约、无人机重叠、电池重叠或能量违规。四种结果的最低返航 SOC 均不低于 30.1478%，高于 20% 安全余量要求。

受限一体化模型稳定得到自己的 76 架次可行解，能耗低于保底解，但加权归一化送达时间和完工时间更差。其余三个入口在短时设置下得到同一个 80 架次保底解，状态均为 `FEASIBLE_FALLBACK`。按统一词典序目标，80 架次保底解当前优于 76 架次一体化解，但由于最佳目标仍来自回退解，不能据此宣称候选法、ALNS 或混合算法的独立寻优性能更强，也不能作为最终论文中的算法优劣结论。

## 2. 统一建模口径

### 2.1 决策对象

- 货箱分配：每个货箱恰好属于一个架次；
- 架次构造：确定机型、服务区访问顺序及各站卸货箱集合；
- 资源分配：每个架次分配一架实体无人机和一块相容电池；
- 时间安排：确定架次准备开始时刻、返航时刻和电池再次可用时刻；
- 配送记录：每箱送达时刻等于架次开始时刻加该箱所在站的架次内送达偏移。

### 2.2 物理与时间约束

- 单架次总质量和总体积不能超过机型容量；
- 每个航段按当前剩余载荷计算等效航程和能量；
- 正反向航段分别计算爬升、下降和巡航时间，不假设方向对称；
- 架次持续时间包括准备、逐箱装载、飞行、站点基础交接和逐箱交接；
- 电池从架次开始被占用，返航后按剩余 SOC 充至 100% 才能再次使用；
- 同一实体无人机的飞行区间不得重叠；
- 同一电池的“飞行 + 充电”区间不得重叠；
- 首批保障截止时间及医疗物资期望时间作为硬时限；其他期望送达时间进入软目标；
- 返航 SOC 不低于机型规定的 20% 安全余量。

### 2.3 统一目标

四种方案统一比较以下词典序目标：

1. 加权归一化送达时间；
2. 总完成时间（makespan）；
3. 总能耗；
4. 总架次数。

本阶段代码直接保存四维目标向量，避免把不同量纲简单相加后掩盖主目标。受限一体化 CP-SAT 内部使用大权重标量化近似该词典序，最终报告仍由公共函数重新计算四维目标。

## 3. 四种 MVP 的实现边界

### 3.1 受限一体化模型 `integrated_milp`

当前实现先生成物理可行候选架次，再由一个 OR-Tools CP-SAT 模型同时决定候选选择、实体无人机、电池和开始时间，并用可选区间与 `NoOverlap` 处理两类可重复资源。

它属于“路线选择—资源调度一体化的受限离散模型”，尚不是阶段二计划中的连续时间 MILP。短时限下未获得可行 incumbent 时，入口会明确返回候选法保底解，并在诊断信息中记录：

- `backend = OR-Tools CP-SAT restricted integrated model`；
- `fallback = candidate_method`；
- `solver_status = FEASIBLE_FALLBACK`。

### 3.2 候选架次法 `candidate`

候选池包含单箱必覆盖候选、第一问动态规划种子和两架次合并候选，经过支配剔除后，以集合划分选择候选，再交给 CP-SAT 调度；若候选组合在资源层不可排程，则加入 no-good cut 后重新选择。

真实数据短时运行中，集合划分选出的合并方案未形成可行资源调度，最终使用按硬时限排序、逐箱选择机型/无人机/电池的资源感知单箱保底构造器。

### 3.3 ALNS `alns`

本阶段实现的是可复现的最小邻域搜索：从有效初解出发，随机选取同机型的两个架次，尝试两种站点顺序的合并，通过公共物理评价器和资源调度器后，仅接受词典序改进方案。固定随机种子时目标可复现。

阶段一尚未实现完整 ALNS 所需的多移除算子、Regret-k 修复、模拟退火劣解接受和算子权重在线学习；诊断字段明确记录请求迭代数、尝试次数和接受次数。

### 3.4 混合算法 `hybrid`

混合算法先运行候选法，随后让 ALNS 从当前可行解出发搜索，将新架次签名回灌候选池，再重新执行集合划分和精确调度。阶段一支持一轮或多轮回灌并记录目标历史。

由于本次短时 ALNS 没有产生改善架次，混合算法仍返回候选保底解。

## 4. 真实数据验收结果

运行命令：

```powershell
python scripts/run_problem_d_q2.py `
  --problem-dir "D:\MProject\mathModelCup\D题" `
  --output-dir "outputs\problem_d_q2" `
  --methods integrated_milp,candidate,alns,hybrid `
  --seed 20260924 `
  --quick `
  --time-limit 5
```

| 方法 | 校验 | 状态 | 加权归一化送达时间 | 完工时间/s | 能耗/kWh | 架次 | 运行时间/s | 回退来源 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| integrated_milp | PASS | FEASIBLE | 1.7068739241 | 37939.8995 | 174.0327 | 76 | 5.560 | 无 |
| candidate | PASS | FEASIBLE_FALLBACK | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 1.425 | resource_aware_single_box |
| alns | PASS | FEASIBLE_FALLBACK | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 1.402 | resource_aware_single_box |
| hybrid | PASS | FEASIBLE_FALLBACK | 1.0945607062 | 18824.5834 | 191.6670 | 80 | 2.312 | resource_aware_single_box |

独立约束复核：

| 指标 | 四种方案结果 |
|---|---:|
| 要求货箱数 | 80 |
| 已配送唯一货箱数 | 80 |
| 缺失货箱数 | 0 |
| 重复货箱数 | 0 |
| 硬时限违约数 | 0 |
| 无人机冲突数 | 0 |
| 电池冲突数 | 0 |
| 能量违规数 | 0 |
| 最低返航 SOC | integrated_milp 为 32.011235%；其余为 30.147820% |

运行时间是一次 Windows 本地短时运行的墙钟时间，只用于确认量级，不适合作为论文统计推断；阶段三应在固定硬件、固定版本和多个随机种子下重复测量。

## 5. 复杂度与时间预估

设货箱数为 \(n\)，服务区数为 \(m\)，候选架次数为 \(K\)，无人机数为 \(U\)，电池数为 \(B\)，ALNS 迭代数为 \(I\)。

- 公共航段矩阵：节点数为 \(m+1\)，时间和空间复杂度均为 \(O(m^2)\)；本题只有 16 个节点，不构成瓶颈。
- 候选生成：单箱候选约为 \(O(3n)\)，朴素两两合并上界约为 \(O(K^2)\)；多站完全枚举随最大停靠数呈组合爆炸。
- 集合划分：二元变量数约为 \(K\)，货箱等式约束为 \(n\)，属于 NP-hard；候选池规模和候选质量比线性代数部分更决定耗时。
- 一体化 CP-SAT：每个候选增加选择、起止时间、最多 \(U+B\) 个资源分配布尔量和可选区间，最坏情形指数复杂；内存近似随 \(K(U+B)\) 增长。
- ALNS：本阶段单次合并评价和重排近似为 \(O(n + K(U+B))\)，总量约为 \(O(I\cdot C_{move})\)；完整版本的修复算子会额外扫描插入位置和机型。
- 混合算法：约等于多轮“ALNS + 扩大候选池后的集合划分/调度”，通常质量更稳，但时间和内存最高。

按当前 80 箱规模估计：

| 阶段 | 工作内容 | 机器运行时间 | 实现与验证时间 |
|---|---|---:|---:|
| 阶段一 | 四个 MVP、公共校验、统一报表 | 快速模式约 20–30 秒/整轮 | 已完成 |
| 阶段二 | 完整四方案独立实现、5-step 比较、去除主要回退 | 预计 0.5–6 小时/参数组 | 约 1–2 个工作日 |
| 阶段三 | 多随机种子、充分测试、敏感性分析、论文级图表 | 预计 6–24 小时，取决于参数网格 | 约 1–2 个工作日 |

以上是工程预估，不是求解器上界。候选数、时间限制、随机种子数和敏感性网格会近似乘法放大总实验时间。

## 6. 输出文件

根目录：

- `method_comparison.csv`：四方案统一指标；
- `summary.md`：自动比较摘要。

每种方法子目录：

- `trips.csv`：架次、资源、时刻、载荷、能量和路径；
- `deliveries.csv`：逐箱送达时间、期望时间和硬时限检查；
- `aircraft_timeline.csv`：实体无人机占用区间；
- `battery_timeline.csv`：电池飞行与充电占用区间；
- `summary.json`：目标、状态和求解诊断；
- `validation.csv`：独立校验问题明细；
- `constraint_check.csv`：论文可直接引用的汇总约束检查。

## 7. 进入阶段二、三前必须增强的内容

1. 扩充多站候选并设计候选规模控制策略，使候选法在资源约束下能稳定产生少于 80 架次的解；
2. 将受限 CP-SAT 与真正的连续时间 MILP 明确区分，补充 incumbent、best bound 和可解释 gap；
3. 完整实现 ALNS 的移除、修复、接受准则和自适应权重，记录收敛曲线；
4. 混合算法维护非支配候选并进行多轮回灌，而非只保留单个当前最优解；
5. 以至少 10 个随机种子重复随机算法，报告均值、标准差、最优值和置信区间；
6. 对安全余量、时限、无人机/电池数量、充电时间、优先权重及候选上限做敏感性分析；
7. 输出甘特图、目标收敛图、Pareto/雷达比较图、敏感性折线或热力图，并统一中文字体、单位、配色和 300 dpi 导出；
8. 用固定环境文件记录 Python、NumPy、SciPy、pandas 和 OR-Tools 版本，确保论文实验可复现。
