# -*- coding: utf-8 -*-
from pathlib import Path
import pandas as pd, json
R=Path(__file__).resolve().parent;Q=R/'q4_results';T=Q/'tables'
g=pd.read_csv(T/'group_summary.csv');r=pd.read_csv(T/'resource_comparison.csv');c=pd.read_csv(T/'partition_comparison.csv');d=pd.read_csv(T/'relay_duplication.csv');s=json.loads((Q/'solver_summary.json').read_text(encoding='utf-8'))
res=['运输机A','运输机B','运输机C','运输电池A','运输电池B','运输电池C','中继无人机','中继能源组件']
def fmt(x):
    if isinstance(x,float):return f'{x:.3f}'
    return str(x)
def mdtable(cols,rows):
    return '|'+ '|'.join(cols)+'|\n|'+'|'.join(['---']*len(cols))+'|\n'+'\n'.join('|'+ '|'.join(fmt(v) for v in row)+'|' for row in rows)

parts=[]
for sc in ['2组','3组']:
    z=g[g.scenario.eq(sc)]
    rows=[]
    for _,x in z.iterrows():rows.append([x.group_id,x.services,int(x.boxes),int(x.transport_trips),x.transport_energy_kwh/1,int(x.relay_task_copies)]+[int(x[q]) for q in res])
    parts.append(f"### {sc}方案\n\n"+mdtable(['组','服务区','箱数','运输架次','运输能耗/kWh','中继副本']+res,rows))
rr=[]
for name in res:
    x2=r[(r.scenario.eq('2组'))&(r.resource.eq(name))].iloc[0];x3=r[(r.scenario.eq('3组'))&(r.resource.eq(name))].iloc[0]
    rr.append([name,int(x2.centralized_required),int(x2.existing_stock),int(x2.partition_total_required),int(x2.redundancy),int(x2.deficit),int(x3.partition_total_required),int(x3.redundancy),int(x3.deficit)])
res_table=mdtable(['资源','集中下界','库存','2组需求','2组冗余','2组缺口','3组需求','3组冗余','3组缺口'],rr)
txt=fr'''# 问题四：救援任务分区与资源配置优化

## 1. 数据继承与建模口径

本问严格继承问题三的22个运输架次、5个中继架次、80个货箱、访问顺序、起止时刻、通信保障对应关系和充电完成时刻，不重新合批或改动时序。资源库存为A/B/C型运输机4/2/2架，A/B/C型共享电池6/4/4组，中继无人机2架，中继能源组件6组。

若原运输架次访问多个服务区，则这些服务区必须属于同组。问题三仅有Q3-T-021同时访问S006和S007，因此二者形成一个不可拆原子单元，其余服务区各自构成原子单元，共14个原子单元。

当一个原中继架次保障的运输任务分属多个组时，各组必须独立执行该中继任务，因而按组复制该架次的悬停点、飞行海拔和服务窗口。复制仅表示独立资源需求，不改变问题三的通信关系和时间安排。

## 2. 分区—资源配置模型

设原子单元集合为 $I$，任务组集合为 $G=\{{1,\ldots,k\}}$，$k\in\{{2,3\}}$；二元变量 $x_{{ig}}$ 表示单元 $i$ 是否属于组 $g$：

$$\sum_{{g\in G}}x_{{ig}}=1,\qquad \sum_{{i\in I}}x_{{ig}}\ge 1.$$

属于同一原运输架次的服务区已通过原子单元合并，所以不会被拆组。组 $g$ 的运输任务集和中继任务集分别为

$$\mathcal T_g=\{{f:\mathcal S_f\cap S_g\ne\varnothing\}},\qquad
\mathcal R_g=\{{r:\mathcal F_r\cap\mathcal T_g\ne\varnothing\}}.$$

对资源类型 $c$，把每项任务转换为占用区间：运输机为 $[t^s_f,t^r_f)$，运输电池为 $[t^s_f,t^{{chg}}_f)$，中继无人机为 $[t^s_r,t^r_r+300)$，中继能源组件为 $[t^s_r,t^{{chg}}_r)$。组内最低资源数等于最大并发占用数：

$$n_{{gc}}=\max_t\sum_{{j\in J_g}}\mathbf 1\{{t\in I_{{jc}}\}}.$$

这是区间图的最小着色数；采用按开始时刻排序、优先复用最早释放资源的区间着色算法，可同时给出达到该下界的具体本组资源编号。分区总需求、相对集中执行的冗余和库存缺口为

$$N_c=\sum_g n_{{gc}},\quad R_c=N_c-n_c^0,\quad D_c=\max(0,N_c-B_c).$$

组间工作量向量取货箱数、运输架次、运输能耗、运输资源忙时、中继任务副本数和中继服务时长。综合不均衡度为

$$B=0.20CV_q+0.15CV_n+0.20CV_E+0.15CV_t+0.10CV_r+0.20CV_h.$$

为避免“一个大组加若干名义小组”的退化解，采用ε约束：两组时每组货箱30–50箱、运输架次8.25–13.75次；三组时每组货箱17.33–36箱、运输架次4.69–9.97次。首先最小化库存相对缺口 $\sum_cD_c/B_c$，再以归一化理想点距离比较资源冗余、工作量不均衡和空间离散度，权重分别为0.40、0.45和0.15。

14个原子单元的无序非空分区全部枚举：两组8191种，三组788970种；满足ε约束的候选分别为3919种和148001种。随后用区间着色生成逐任务资源配置，并独立复核互斥关系。

## 3. 分区与逐组资源配置

{chr(10).join(parts)}

完整的任务—本组资源编号—占用起止时刻见 `tables/task_resource_allocation.csv`。S006与S007在两种方案中始终同组，满足Q3-T-021不可拆分约束。

## 4. 资源规模、冗余与库存缺口

{res_table}

集中执行时最低需26个资源单元。两组独立执行共需30个，产生4个冗余单元，具体为中继无人机增加2架、中继能源组件增加2组；三组共需35个，产生9个冗余单元。资源总数与库存总数相同并不代表可行，因为不同类型资源不能互相替代。

两组方案只缺中继无人机2架：两个组各自都出现2架中继机的并发峰值，组间又禁止调配，故需求为4架而库存仅2架。Q3-R-001和Q3-R-003均因其保障的运输架次跨组而各复制1次，这是中继冗余的直接来源。能源组件需要5组，库存6组，仍有1组余量。

三组方案缺B型运输机1架、C型运输机1架、B型电池1组、C型电池1组和中继无人机2架，共6个不可替代的类别缺口。各组独立保留原时序后，B、C型任务在不同组分别形成并发峰值，原来可以跨服务区复用的机体和电池被分割；电池占用一直延续到充满，更容易累加峰值。Q3-R-001跨G2/G3、Q3-R-003跨G1/G2，均需复制；三组中继机峰值之和为4架。中继能源组件恰需6组，等于库存。

## 5. 工作量均衡及方案比较

两组方案的货箱量为40/40，运输架次为10/12，运输能耗为31.899/35.406kWh，综合不均衡度为0.102；主要不均衡来自中继任务副本5/2。三组方案货箱量为27/35/18，运输架次为7/8/7，运输架次更均匀，但货箱、中继服务时长差异较大，综合不均衡度上升至0.177。

三组空间离散度比为0.501，优于两组的0.985，说明增加分组更容易形成空间上相对集中的执行单元；代价是资源池进一步碎片化，冗余由4增至9，库存缺口由2增至6。因此现有库存条件下，推荐两组方案：它兼顾总工作量均衡与较小资源扩充，只需增配2架中继无人机。若更看重属地化、较短的组内地理跨度且可以增配多类型资源，可采用三组方案。

不加工作量ε约束时，资源缺口的理论下界分别为相对缺口0.75和1.50，但对应14∶1及13∶1∶1的服务区退化分区，不适合作为独立救援单元。本文用少量资源代价换取可执行的工作量均衡。

## 6. 可行性检验

独立校验16项全部通过：15个服务区唯一覆盖、S006—S007同组、22个运输架次完整且唯一归组、80箱不重不漏、中继复制关系正确、每个本组实体资源的占用区间无重叠、区间着色编号数等于峰值需求、资源总量/冗余/缺口算术一致。这里的“资源可行”指按表中配置数量可无冲突执行；相对现有库存，两种方案都需先补足上述缺口。

## 7. 结果图

![2组与3组任务分区](figures/01_任务分区.png)

![资源规模与库存缺口](figures/02_资源规模与缺口.png)

![组间工作量均衡](figures/03_工作量均衡.png)
'''
(Q/'问题四_模型建立与求解.md').write_text(txt,encoding='utf-8');print(Q/'问题四_模型建立与求解.md')
