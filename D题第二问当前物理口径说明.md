# D题第二问当前物理口径说明

## 1 文档用途

本文档说明当前代码实际采用的 GIS、飞行时间、载荷、能耗、SOC、充电、资源占用和送达时限口径。目标是让论文公式、程序实现、结果解释和独立校验保持一致；本文描述的是“当前已实现模型”，不是对真实无人机系统的完整刻画。

当前主要实现文件为：

- src/math_model_cup/problem_d_q2.py：数据结构与 Excel 数据加载；
- src/math_model_cup/problem_d_q2_geometry.py：DEM 航段几何；
- src/math_model_cup/problem_d_q2_physics.py：架次时间、能耗、SOC、充电和目标计算；
- src/math_model_cup/problem_d_q2_schedule.py：无人机与电池资源排程；
- src/math_model_cup/problem_d_q2_validation.py：独立复算与约束校验。

## 2 符号、初始状态与时间基准

设：

- \(O01\) 为唯一调度中心；
- \(i,j\) 为调度中心或服务区节点；
- \(g\) 为无人机型号；
- \(u\) 为实体无人机；
- \(k\) 为实体电池；
- \(b\) 为不可拆分物资箱；
- \(p\) 为一次从 \(O01\) 出发、访问一个或多个服务区、最后返回 \(O01\) 的完整架次；
- \(q\) 为某航段开始时的剩余载荷质量。

所有架次均从 \(O01\) 出发并返回 \(O01\)。数据中的实体无人机初始节点会被读取，但当前第二问排程没有建立异地调机过程，因此实际口径等价于所有可用实体无人机在时刻 0 位于调度中心。所有共享电池在时刻 0 均以 100% SOC 位于 \(O01\)。每个架次只分配并携带一块兼容工作电池，不允许无人机随身携带备用电池。

所有输入的送达/截止时刻与输出时刻的共同零点均为调度开始时刻，单位为秒。期望送达时间、首批截止时间、架次开始时间和实际送达时间均解释为“自调度开始起经过的秒数”，可以直接比较；准备、装载、飞行、交接和充电则是持续时长。

## 3 GIS 与航段几何口径

### 3.1 水平坐标与距离

DEM 的水平坐标参考系必须为 WGS84，即 EPSG:4326。节点间水平距离 \(d_{ij}\) 使用两个经纬度端点之间的大地椭球距离，不使用简单平面欧氏距离。

当前距离是端点之间的水平大地距离，不是沿地表起伏计算的三维曲线长度。

### 3.2 垂直基准假设

程序把节点高程与 DEM 栅格值直接相减，因此默认二者单位均为米，且采用兼容的垂直高程基准。程序没有读取或转换独立的垂直坐标参考系。

如果节点高程与 DEM 使用不同的高程基准，爬升量、下降量、飞行时间和爬升能耗都会出现系统偏差。这是当前 GIS 口径中必须在论文里披露的输入一致性假设。

### 3.3 DEM 取样

对每一对不同节点，程序在两节点连线经过的 DEM 栅格中取样，过滤无效值与 NoData，并取有效栅格中的最高地面高程：

$$
h^{\max}_{ij}
=\max\{h(r,c):(r,c)\text{ 位于 }i\text{ 与 }j\text{ 的栅格连线上}\}.
$$

若整条连线没有有效 DEM 栅格，该航段直接报错，不使用零高程或邻近值替代。

### 3.4 操作高度与巡航高度

调度中心的操作高度取其地面高程：

$$
h_i^{\mathrm{op}}=h_i,\qquad i=O01.
$$

服务区的操作高度取地面高程上方 30 m：

$$
h_i^{\mathrm{op}}=h_i+30,\qquad i\ne O01.
$$

每条航段的巡航高度取连线最高地面高程上方 50 m：

$$
h_{ij}^{\mathrm{cruise}}=h^{\max}_{ij}+50.
$$

由此定义该方向的爬升量与下降量：

$$
H^{\uparrow}_{ij}
=\max\left(0,h_{ij}^{\mathrm{cruise}}-h_i^{\mathrm{op}}\right),
$$

$$
H^{\downarrow}_{ij}
=\max\left(0,h_{ij}^{\mathrm{cruise}}-h_j^{\mathrm{op}}\right).
$$

同一对节点的水平距离和最高地面高程相同，但起点、终点操作高度的角色互换，因此两个方向分别存储和计算。这里的方向差异只来自“爬升起点”和“下降终点”的互换，不表示模型考虑了风场、单向航路或方向性避障。

### 3.5 当前地形近似的准确含义

当前实现是“端点大地距离 + 沿线最高 DEM 地面高程 + 单次爬升/下降”的航段近似：

- 不累计 DEM 剖面上的全部局部爬升与下降；
- 不计算绕开山体、禁飞区或障碍物的三维最短航路；
- 不采用贴地飞行或随地形连续变高的轨迹；
- 不包含风场导致的方向性航速与能耗变化。

论文宜表述为“基于沿线 DEM 栅格最高地面高程的巡航高度近似”，不宜表述为“精确累计地形爬升”。所谓“保守”仅相对于已抽取到的 DEM 地面栅格值而言，其程度取决于 DEM 分辨率和栅格连线抽样结果，且不覆盖建筑、植被、电线等非地形障碍物。

## 4 装载、访问与卸货口径

### 4.1 物资不可拆分与唯一归属

每个物资箱不可拆分，必须完整交付到数据指定的服务区。同一架次中：

- 至少访问一个服务区；
- 至少携带一个物资箱；
- 同一物资箱只能出现一次；
- 同一服务区最多出现一次，但可在该停靠点同时卸下多个箱；
- 所有箱的目的服务区必须与对应停靠点一致。

### 4.2 起飞容量约束

设架次 \(p\) 携带的箱集合为 \(B_p\)，则起飞总质量和总体积为

$$
Q_p=\sum_{b\in B_p}m_b,
\qquad
V_p=\sum_{b\in B_p}v_b,
$$

并满足

$$
Q_p\le Q_g^{\max},
\qquad
V_p\le V_g^{\max}.
$$

体积只在起飞装载时检查；质量会在每次卸货后递减，并影响后续航段的等效航程和能耗。

### 4.3 逐段载荷递减

首段载荷等于全部起飞载荷。到达一个服务区并完成该站全部交接后，相关物资质量从剩余载荷中扣除。最后一个服务区返回 \(O01\) 的航段按空载 \(q=0\) 计算。

同一停靠点内的多个箱视为在该站完整交接结束时同时送达，不区分站内逐箱先后顺序。

## 5 时间口径

### 5.1 航段飞行时间

型号 \(g\) 在航段 \(i\rightarrow j\) 上的时间为

$$
t_{ijg}
=\frac{H^{\uparrow}_{ij}}{v_g^{\uparrow}}
+\frac{d_{ij}}{v_g^{\mathrm{cruise}}}
+\frac{H^{\downarrow}_{ij}}{v_g^{\downarrow}}.
$$

模型把爬升、水平巡航和下降视为依次发生的三个阶段。水平距离不随载荷变化，航速也不随载荷、风或电量变化。

### 5.2 架次准备、装载与交接

若架次携带 \(n_p\) 个箱，则起飞前时间为

$$
t_p^{\mathrm{pre}}
=t_g^{\mathrm{prepare}}+n_p t_g^{\mathrm{load/box}}.
$$

在服务区 \(s\) 交接 \(n_{ps}\) 个箱的时间为

$$
t_{ps}^{\mathrm{handoff}}
=t_g^{\mathrm{handoff,base}}
+n_{ps}t_g^{\mathrm{handoff/box}}.
$$

物资的送达时刻定义为该服务区完整交接结束时，而不是无人机刚到达服务区时。

### 5.3 架次持续时间

架次总持续时间包括：

1. 起飞前准备时间；
2. 按箱计的装载时间；
3. 各航段爬升、巡航和下降时间；
4. 各停靠点基础交接时间和逐箱交接时间；
5. 最后一站至 \(O01\) 的空载返航时间。

当前没有额外加入天气等待、空域排队、故障缓冲、返航后检修或电池拆装时间。

## 6 载荷相关航程口径

型号 \(g\) 的空载航程为 \(R_g^0\)，满载航程为 \(R_g^F\)，最大载荷为 \(Q_g^{\max}\)。载荷为 \(q\) 时，等效航程采用 1.5 次幂插值：

$$
R_g(q)
=R_g^0-\left(R_g^0-R_g^F\right)
\left(\frac{q}{Q_g^{\max}}\right)^{1.5}.
$$

该关系满足

$$
R_g(0)=R_g^0,
\qquad
R_g(Q_g^{\max})=R_g^F.
$$

载荷必须位于 \([0,Q_g^{\max}]\) 内，否则架次被判为不可行。

## 7 能耗口径

### 7.1 水平航程能耗

设型号 \(g\) 的输入可用电池能量为 \(E_g^{\mathrm{use}}\)，航程—能量标定系数为 \(\alpha\)。载荷为 \(q\) 的航段水平能耗为

$$
E_{ijg}^{\mathrm{horizontal}}(q)
=\alpha E_g^{\mathrm{use}}\frac{d_{ij}}{R_g(q)}.
$$

基准口径取 \(\alpha=1.0\)，表示当前载荷下飞完等效航程恰好消耗全部可用能量。备选口径取 \(\alpha=0.8\)，表示表中标准航程已经包含 20% 返航电量余量，即飞完标准航程只消耗 80% 的可用能量。第二问敏感性分析会在两种口径下分别重新生成和优化架次，而不是在基准结果上事后缩放总能耗。

论坛专家没有确认水平航程与爬升附加能耗的唯一分项公式。因此，本节公式属于为补足题面信息而建立的可复现物理假设，不是“题目原式”或“专家唯一指定公式”。\(\alpha=1.0\) 仍是当前发布基准，\(\alpha=0.8\) 仅作为备选解释与稳健性检验。

\(E_g^{\mathrm{use}}\) 在当前代码中直接作为水平能耗标定量、SOC 分母和能量约束基数；程序不会先从中扣除安全余量。安全余量 \(\rho\) 在约束 \(E_p\le(1-\rho)E_g^{\mathrm{use}}\) 中单独施加，因此没有重复扣减。

### 7.2 爬升能耗

爬升能耗采用重力势能除以爬升效率：

$$
E_{ijg}^{\mathrm{climb}}(q)
=\frac{(m_g^{\mathrm{empty}}+q)g_0H^{\uparrow}_{ij}}
{3.6\times10^6\eta_g^{\mathrm{climb}}},
$$

其中 \(g_0\) 为重力加速度，\(3.6\times10^6\) 用于将焦耳换算为 kWh。

### 7.3 航段与架次总能耗

航段总能耗为

$$
E_{ijg}(q)
=E_{ijg}^{\mathrm{horizontal}}(q)
+E_{ijg}^{\mathrm{climb}}(q).
$$

架次总能耗为全部去程、站间和空载返航航段能耗之和。

当前能耗模型未单独计入：

- 下降阶段能耗或能量回收；
- 悬停、交接、起飞前准备和装载能耗；
- 风、降雨、温度、电池老化及电机效率随工况的变化；
- 通信设备、载荷保温或其他任务设备能耗；
- 起降瞬态功率峰值。

## 8 SOC 与安全余量

每个架次假定起飞时电池 SOC 为 100%。若架次总能耗为 \(E_p\)，则返航 SOC 为

$$
SOC_p^{\mathrm{return}}
=100\left(1-\frac{E_p}{E_g^{\mathrm{use}}}\right)\%.
$$

给定安全余量 \(\rho\) 时，能耗必须满足

$$
E_p\le(1-\rho)E_g^{\mathrm{use}}.
$$

当前基准计算显式传入统一安全余量 \(\rho=20\%\)。虽然各机型数据中也读取了安全余量字段，但该字段目前不会自动覆盖物理评估参数；架次可行性实际以函数参数 reserve_ratio 为准，基准值为 20%，敏感性分析会显式改变该参数。

## 9 电池充电口径

电池从返航 SOC \(s\in[0,1]\) 充至 100% 的时间采用连续分段线性模型。设从 0% 充至 100% 的标称时间为 \(T_k^{\mathrm{full}}\)。

当 \(s<0.9\) 时：

$$
T_k^{\mathrm{charge}}(s)
=T_k^{\mathrm{full}}
\left[
0.65\frac{0.9-s}{0.9}+0.35
\right].
$$

当 \(s\ge0.9\) 时：

$$
T_k^{\mathrm{charge}}(s)
=T_k^{\mathrm{full}}
\left[0.35\frac{1-s}{0.1}\right].
$$

该模型意味着 0%–90% SOC 区间占完整充电时间的 65%，90%–100% 区间占 35%，并保证在 90% 处连续。

当前不允许部分充电后提前复飞；每次使用后的电池必须充至 100% 才重新可用。

专家答疑允许在“已有兼容电池的地点”换电，但没有明确服务区电池的转运方式、充电条件、换电耗时和再次起飞规则。为避免无依据地在服务区重置 SOC，当前基准模型保守限制所有换电和充电均在 \(O01\) 完成，不建立异地电池库存或中途换电状态。该限制会收缩专家答疑所允许的潜在可行域，因此本文的可行性与最好已知解结论只适用于这一收缩模型。

## 10 无人机与电池资源占用

对架次 \(p\)，设排程开始时刻为 \(T_p\)，持续时间为 \(D_p\)，充电时间为 \(C_p\)。资源占用区间为

$$
\text{无人机占用}=[T_p,T_p+D_p),
$$

$$
\text{电池占用}=[T_p,T_p+D_p+C_p).
$$

这表示：

- 无人机返航后即可执行下一架次；
- 电池返航后仍持续占用，直到充满；
- 同一实体无人机的占用区间不得重叠；
- 同一实体电池的飞行与充电占用区间不得重叠；
- 无人机型号、电池型号和架次机型必须一致。

由于准备和装载时间已包含在 \(D_p\) 内，无人机和电池从准备开始时就被占用。

模型没有单独建立装载人员、地勤人员和充电桩资源。就充电而言，这等价于假设所有返航电池都能立刻接入充电，充电能力无限并行，不会因充电桩不足排队。

贪心排程保留浮点秒。CP-SAT 中的排程开始时刻为整数秒，架次持续时间和充电时间向上取整到整数秒建立 NoOverlap，因此资源冲突判断相对保守；最终报告再以该开始时刻结合物理核的浮点持续时间、能耗和送达偏移复算，可能保留不到 1 秒的离散化空隙。

## 11 送达时限口径

每个箱 \(b\) 有期望送达时间 \(\tau_b^{\mathrm{exp}}\)。硬截止时间按以下规则生成：

1. 当输入列“是否首批保障”等于“是”，且“首批截止时间（s）”非空时，该截止时间是硬约束候选；
2. 当输入列“物资类型”严格等于“医疗物资”时，其“期望送达时间（s）”同时作为硬约束候选；
3. 若同时存在多个候选，取最小值；
4. 其他箱的期望送达时间只进入目标函数，不作为硬不可行条件。

因此

$$
\tau_b^{\mathrm{hard}}
=\min\left(
\{\tau_b^{\mathrm{first}}:b\text{ 为首批保障且首批截止非空}\}
\cup
\{\tau_b^{\mathrm{exp}}:b\text{ 为医疗物资}\}
\right).
$$

若候选集合为空，该箱没有硬截止时间。

箱 \(b\) 的实际送达时刻为

$$
T_b^{\mathrm{delivery}}
=T_p+\delta_{pb},
$$

其中 \(\delta_{pb}\) 包括起飞前准备、装载、此前所有航段与交接，以及本停靠点的完整交接时间。硬时限要求

$$
T_b^{\mathrm{delivery}}\le\tau_b^{\mathrm{hard}}.
$$

## 12 最终评价目标

四方案最终统一比较真实目标向量

$$
\min (Z_1,M,E,K),
$$

并采用词典序排序。

### 12.1 加权归一化送达时间

$$
Z_1
=\frac{
\sum_b w_b\dfrac{T_b^{\mathrm{delivery}}}{\tau_b^{\mathrm{exp}}}
}{\sum_b w_b},
$$

其中 \(w_b\) 为应急优先系数。

### 12.2 完工时间

$$
M=\max_p T_p^{\mathrm{return}}.
$$

完工时间取最后一架无人机返航时刻，不包含其返航后的最后一次充电完成时间。因此，\(M\) 不是“全部电池资源释放”的时刻。

### 12.3 总飞行能耗

$$
E=\sum_p E_p.
$$

该指标只累计飞行物理核中的水平航程能耗和爬升能耗，不包含充电损耗及地面能耗。

### 12.4 架次数

$$
K=|P|.
$$

词典序意味着先比较 \(Z_1\)，只有前一层完全相同时才比较下一层。因此，架次数更少不自动表示方案在当前发布目标下更优。

## 13 独立校验口径

每个最终解都由独立校验器重新执行以下检查：

1. 实体无人机和电池编号存在且机型兼容；
2. 使用原始停靠顺序重新计算架次质量、体积、时间、能耗、返航 SOC 和逐箱送达偏移；
3. 记录值与重新计算值在数值容差内一致；
4. 每个物资箱恰好送达一次，无遗漏、无重复、无额外箱；
5. 箱、架次和服务区引用一致；
6. 所有硬截止时间满足；
7. 同一无人机不存在占用重叠；
8. 同一电池在飞行和充电期间不存在占用重叠；
9. 最终四维目标与明细重新计算结果一致。

独立校验通过只能证明方案满足当前模型及数值口径，不能证明模型完整覆盖真实飞行环境，也不能证明该方案为全局最优。

## 14 当前物理口径未包含的因素

以下因素当前没有进入模型：

- 实时或随机风场、降雨、能见度和温度；
- 山体侧向绕飞、建筑物、电线和动态禁飞区；
- 通信覆盖、链路中断和中继约束；
- 起降场容量、充电桩数量、装卸人员和地面排队；
- 电池健康度、倍率效应、充放电效率和循环衰减；
- 无人机故障、任务取消和动态新增需求；
- 多基地、异地备降、中途换电和部分充电；
- 飞行控制层面的加速度、转弯半径、姿态和瞬时功率约束。

因此，当前结果适合用于数学建模竞赛中的统一比较、算法实验和方案筛选。若用于实际执行，应进一步引入气象、空域、通信、飞控和现场资源数据，并进行安全审查。

## 15 当前基准结果的物理校验摘要

当前 20% 安全余量基准解来自 `outputs/problem_d_q2_stage3/baseline/` 中四方案统一比较的共同发布解；`outputs/problem_d_q2_stage3/` 根目录下的 24 架次 Hybrid 结果仅作为历史搜索记录，不作为正式发布结果。基线比较中四个方法最终都保留了同一已知可行 incumbent。该解为 22 架次，80 个物资箱全部且仅送达一次。独立校验结果为：

- 硬时限违反数：0；
- 无人机重叠数：0；
- 电池重叠数：0；
- 能量约束违反数：0；
- 最低返航 SOC：21.4675%；
- 四维目标：

$$
(Z_1,M,E,K)
=(0.5163286171,\ 9408.4439\text{ s},\ 64.8317281\text{ kWh},\ 22).
$$

“最好已知”的比较范围仅限本仓库本轮四种实现、给定候选空间、随机种子、参数和计算预算。当前输出没有给出覆盖完整决策空间的有效下界、最优性间隙或穷尽性证明，因此该结果是当前实验范围内的最好已知可行解，不是全局最优证明。

### 15.1 航程—能量标定对照

在其他设置不变的条件下，分别使用两种标定口径重新生成并优化架次，结果如下：

| 标定系数 \(\alpha\) | 物理解释 | \(Z_1\) | 完工时间（s） | 总飞行能耗（kWh） | 架次数 | 最低返航 SOC |
|---:|---|---:|---:|---:|---:|---:|
| 1.0 | 标准航程消耗全部可用能量，当前基准 | 0.5163286171 | 9408.4439 | 64.8317281 | 22 | 21.4675% |
| 0.8 | 标准航程已包含 20% 返航余量，备选解释 | 0.5148673648 | 9293.9595 | 52.4158746 | 22 | 36.8757% |

两个场景均通过独立物理与资源校验。0.8 场景保留 1.0 场景的 22 架次运输结构，按新口径重算能耗、SOC 与充电时间后重新排程，并与 0.8 场景的独立启发式搜索结果按四维词典序择优。结果表明，当前最好已知的 22 架次结构在两种标定口径下均可行；较低的水平能耗使电池更早恢复可用，从而改善主目标和完工时间。总能耗、最低 SOC 和具体排程仍依赖标定解释，且该对照不构成任一口径下的全局最优证明。
