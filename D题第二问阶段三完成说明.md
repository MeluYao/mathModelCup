# D题第二问阶段三完成说明

## 1 完成状态

阶段三及本次四算法缺陷修复已经完成：

- 共同分组解已作为四算法的原生初始 incumbent；
- 受限 CP-SAT 保留种子候选并使用求解提示；
- 候选法把种子架次并入列池；
- ALNS 和混合算法从共同种子继续搜索；
- 已删除报告层 `shared_grouped_local_search` 结果覆盖；
- 已统一按 $(Z_1,M,E,K)$ 比较最终可行解；
- 已重跑四算法、10 个随机种子、14 个敏感性场景和 7 张论文图；
- 阶段三输出中没有空文件，也没有旧共享覆盖来源。

## 2 四方案结果

| 方法 | 校验 | $Z_1$ | 完工时间/s | 能耗/kWh | 架次 | 时间/s | 来源 | 种子状态 |
|---|---:|---:|---:|---:|---:|---:|---|---|
| 受限一体化 CP-SAT | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 4.802 | `provided_seed` | 保留 |
| 候选架次法 | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 5.394 | `provided_seed` | 保留 |
| ALNS | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 2.745 | `provided_seed` | 保留 |
| 混合算法 | PASS | 0.516329 | 9408.44 | 64.8317 | 22 | 7.744 | `provided_seed` | 保留 |

修复后的四种终值仍然相同，但原因已改变：共同种子真实进入四个原生求解器，四者在当前预算内均未严格改进，并各自记录 `seed_retained=true`；报告层没有再覆盖结果。目标并列时 ALNS 运行最快，因此本轮推荐 ALNS 作为主搜索入口。50 次和 200 次混合迭代的补充检查也未改进种子。

## 3 缺陷修复情况

| 原缺陷 | 修复措施 | 状态 |
|---|---|---:|
| 四算法在报告层被统一替换 | 删除后处理覆盖，只传递初始种子 | 已解决 |
| CP-SAT 候选截断会丢失优质组合 | 种子候选受保护并加入 hint | 已解决 |
| 候选法回退到单箱解 | 种子进入候选池并作为 incumbent | 已解决 |
| ALNS 默认从 80 架次开始 | 报告入口显式传入 22 架次种子 | 已解决 |
| 混合算法没有共同上界 | 候选法、ALNS、刷新轮均继承种子 | 已解决 |
| 搜索目标与报告目标不一致 | 共同种子改用 published 目标序 | 已解决 |
| `own_incumbent` 语义失真 | 新增 `seed_retained`、`native_improved_seed`、`incumbent_source` | 已解决 |
| 图表偶发 Windows IO 失败 | 图表保存增加三次有界重试并测试 | 已解决 |

仍然存在的是算法能力边界，而不是上述实现缺陷：候选池不完备、CP-SAT 未证明原问题 gap=0、ALNS quick 预算较小。这些限制决定当前结果不能宣称全局最优。

## 4 自动化测试

新增和更新的测试覆盖：

1. 候选法接收种子并保留种子架次；
2. CP-SAT 在候选上限小于种子架次数时仍保留全部种子；
3. ALNS 和混合算法记录种子是否被改进；
4. 报告输出种子来源和 incumbent 语义；
5. 真实数据四算法均通过独立约束校验且目标不劣于种子；
6. 图表保存遇到一次瞬时 `OSError` 时可恢复；
7. 原有数据、物理、调度、实验和输出测试不回归。

最终全量回归结果为 **59 passed、1 skipped、0 failed**，耗时 104.11 s。两条警告均来自第三方依赖的弃用提示，不影响算法与校验结果。

## 5 重复实验与敏感性分析

ALNS 使用 10 个固定随机种子，每个运行 20 次迭代：10/10 通过校验，$Z_1$ 均值为 0.5163286171、标准差为 0，平均搜索时间 0.2731 s；所有 quick 重复均保留共同种子。

敏感性分析共 14 个场景，13 个找到可行解。资源降至原配置 50% 时当前启发式未找到满足硬时限的排程；这不构成数学上的不可行证明。

## 6 最优性结论

当前可以证明输出方案在已建模型下可行，也可以证明四个原生入口都在相同种子上完成了各自搜索。不能证明该种子是全局最优解，因为受限候选模型没有覆盖全部可行架次，也没有提供原问题的匹配下界或 gap=0 证书。

论文中应使用“当前最好已知可行解”“本预算下四算法均未改进共同种子”，不应使用“已证明最优”。

## 7 输出位置

- 主比较：[outputs/problem_d_q2_stage3/baseline/method_comparison.csv](outputs/problem_d_q2_stage3/baseline/method_comparison.csv)
- 重复实验：[outputs/problem_d_q2_stage3/alns_repetitions.csv](outputs/problem_d_q2_stage3/alns_repetitions.csv)
- 敏感性分析：[outputs/problem_d_q2_stage3/sensitivity_analysis.csv](outputs/problem_d_q2_stage3/sensitivity_analysis.csv)
- 图表：[outputs/problem_d_q2_stage3/figures](outputs/problem_d_q2_stage3/figures)
- 完整方法报告：[D题第二问方法与方案结果报告.md](D题第二问方法与方案结果报告.md)
