# D题第二问四算法原生种子化实施计划

1. 增加统一解比较与种子诊断辅助函数，并以单元测试冻结语义。
2. 为候选架次法增加 `initial_solution`，确保种子架次进入候选池并作为 incumbent。
3. 为一体化 CP-SAT 增加种子候选保护、求解提示和种子保留逻辑。
4. 调整 ALNS 与混合算法，使共同种子进入原生搜索流程并使用统一目标更新。
5. 删除报告层共享解覆盖，修正 `own_incumbent` 等输出字段及说明文字。
6. 先运行定向测试，再运行第二问完整测试；根据失败逐项修复。
7. 重跑真实数据四算法、重复实验和敏感性分析，核验输出非空且约束通过。
8. 更新方法结果报告与论文正文，说明修复后的效果、局限和最优性结论。

